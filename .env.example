# Configurações do VS Code Tunnel Pod
# Copie este arquivo para .env e personalize conforme necessário

# Portas
VSCODE_PORT=8000
TUNNEL_PORT=8080

# Usuário (obtenha com: id -u e id -g)
USER_UID=1000
USER_GID=1000

# Nome do tunnel (deve ser único)
TUNNEL_NAME=dev-tunnel

# Modo de servir (serve-web ou serve-local)
VSCODE_SERVE_MODE=serve-web

# Configurações de proxy (se necessário)
#HTTP_PROXY=http://proxy.empresa.com:8080
#HTTPS_PROXY=http://proxy.empresa.com:8080
#NO_PROXY=localhost,127.0.0.1

# Configurações de display (para GUI se necessário)
#DISPLAY=:0
